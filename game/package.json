{
  "name": "blackout-protocol",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "BLACKOUT PROTOCOL — a single-player 3D FPS for the browser, built with Three.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.166.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
